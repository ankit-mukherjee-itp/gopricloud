# Copy to .env (or export directly) before running cmd/

# Required
DATABASE_URL=postgres://postgres:admin@localhost:5432/gopricloud?sslmode=disable
JWT_SECRET=akmbuildscodeplatforminthehome
# Optional (defaults shown)
PORT=8080
JWT_ISSUER=gopricloud