# Copy to .env (or export directly) before running cmd/api.

# Required
DATABASE_URL=postgres://postgres:admin@localhost:5432/gopricloud?sslmode=disable
JWT_SECRET=change-me-to-a-long-random-value

# Optional (defaults shown)
PORT=8080
JWT_ISSUER=gopricloud
